# This file makes Python treat the 'network' directory as a package.

from .federation_protocol import (
    FederatedTaskRequest,
    FederatedTaskResponse,
    FederationNode,
    FederationManager
)
